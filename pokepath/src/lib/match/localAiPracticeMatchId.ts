/** Local vs-computer practice; never persisted as online match. */
export const LOCAL_AI_PRACTICE_MATCH_ID = 'local-ai-practice' as const

/** Stable synthetic id for player2 in AI practice (not a Supabase user). */
export const LOCAL_AI_OPPONENT_ID = 'local-ai-opponent' as const
