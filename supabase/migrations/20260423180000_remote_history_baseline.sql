-- Baseline: this version exists in hosted `supabase_migrations` history without a prior file in git.
-- Keeps local migration filenames aligned with the remote database for `supabase db push`.
-- No-op; use `supabase db pull` if you need to capture real DDL that ran under this version.
SELECT 1;
